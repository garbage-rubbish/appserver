<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.com.cybertech.sdly.mapper.UserMapper">
    <select id="selectById" resultType="user" parameterType="String">
        select * from user where id=#{id}
    </select>
    <insert id="insert" parameterType="user" >
       <!-- <selectKey keyProperty="id" resultType="String">
            select replace(uuid(),'-','')
        </selectKey>-->
        insert into user(id,nickname,gender,avatar,type,status,create_time,update_time)
        values(#{id},#{nickname},#{gender},#{avatar},#{type},#{status},#{createTime},#{updateTime})
    </insert>
</mapper>